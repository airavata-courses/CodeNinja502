apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-green
  labels:
   app: ui
spec:
  replicas: 1
  selector:
   matchLabels:
    app: ui
  template:
    metadata:
      labels:
        app: ui
        color: green 
    spec:
      containers:
      - name: ui
        image: pmorpari/ui-green
        imagePullPolicy: Always
        ports:
        - containerPort: 3000

